import { WizardContext } from '@stencil/cli';

export declare const wizard: {
    init: {
        id: string;
        displayName: string;
        description: string;
        run({ config, workspaceRoot, prompts, nypm, openStencilConfig }: WizardContext): Promise<void>;
    };
};
